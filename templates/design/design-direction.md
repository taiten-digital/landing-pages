# Design Direction — {{CLIENT_NAME}}

## Design Read
One-liner: what should this page feel like, and why (tie back to Brand DNA
and audience).

## Token Overrides
Values to set in `clients/{{SLUG}}/src/tokens.css` (colors, type, spacing,
radius) and why each departs from the shared defaults.

## Reference Moodboard
Links/notes gathered during visual research — inspiration only; note which
assets are legally reusable vs. reference-only.

## Layout & Motion Notes
Grid, imagery treatment, any motion (defer to the `animate` skill for
implementation specifics).
