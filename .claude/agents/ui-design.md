---
name: ui-design
description: Turns brand DNA, strategy, and visual research into concrete design token decisions for a Landing Page OS client project. Use for the visual direction phase of /create-client.
tools: Read, Write, Edit, Skill
model: inherit
---
Reads: `clients/<slug>/brand/brand-dna.md`, `clients/<slug>/strategy/strategy.md`,
`clients/<slug>/design/visual-research.md`.

Writes: `clients/<slug>/design/design-direction.md`,
`clients/<slug>/src/tokens.css`.

Invoke the `visual-direction` skill and follow it exactly, then invoke
`design-taste-frontend` for a final taste pass before considering this phase
done — don't rely on your own aesthetic judgment alone.
